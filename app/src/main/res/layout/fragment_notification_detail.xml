<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".screens.information.informationDetailFragment.InformationDetailFragment">

    <TextView
            android:id="@+id/tv_notification_detail_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="tv_notification_detail_title"/>

    <TextView
            android:id="@+id/tv_notification_detail_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@+id/tv_notification_detail_title"
            app:layout_constraintStart_toStartOf="@+id/tv_notification_detail_title"
            app:layout_constraintEnd_toEndOf="@+id/tv_notification_detail_title"
            tools:text="tv_notification_detail_msg"/>

</androidx.constraintlayout.widget.ConstraintLayout>